import { types } from "./types";
import { muatations } from "./mutations";
import { resolvers } from "./resolvers";
import { queries } from "../buzz/queries";
export const Tweet = { types, queries, muatations, resolvers };
